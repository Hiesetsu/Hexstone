/// @description Insert description here
// You can write your code in this editor
hovered = false;
if(!move_node&&!attack_node&&!target_node)
	image_blend = startColor;
if(model != noone)
{
	ex_hide_card(model);
}